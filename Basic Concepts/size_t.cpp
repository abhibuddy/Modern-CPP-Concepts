/**
 * size_t gurantee the size large enough to hold
 * any value only condition is that he value should not be negative.
*/
#include <iostream>

using namespace  std;

int main()
{
    size_t length = 500;
    std::cout<<"The length is : "<<length<<std::endl;
    return 0;
}